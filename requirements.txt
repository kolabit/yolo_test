streamlit>=1.28.0
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
Pillow>=10.0.0
pandas>=2.0.0
numpy>=1.24.0
pydantic>=2.0.0
# OpenCV - try headless version if regular version fails
opencv-python-headless>=4.8.0
# Alternative: opencv-python>=4.8.0  # Uncomment if headless doesn't work 